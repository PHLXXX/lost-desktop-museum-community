import { mkdir, readFile, writeFile } from 'node:fs/promises'
import { dirname, join } from 'node:path'
import { publisherSchema, registryCaseSchema, registryIndexSchema } from '../schema/catalogSchemas'

export interface BuildSiteOptions { outputRoot: string }
const escape = (value: unknown) => String(value ?? '').replace(/[&<>"']/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' })[character]!)
async function put(path: string, value: string) { await mkdir(dirname(path), { recursive: true }); await writeFile(path, value) }
function layout(title: string, content: string, depth = 0) {
  const root = depth ? '../'.repeat(depth) : './'
  return `<!doctype html>\n<html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escape(title)} · 档案交换站</title><link rel="stylesheet" href="${root}assets/community.css"></head><body><header><a href="${root}index.html">ARCHIVE EXCHANGE</a><nav aria-label="社区导航"><a href="${root}index.html">案件目录</a><a href="${root}submission.html">投稿说明</a><a href="${root}trust.html">信任模型</a><a href="${root}content-guidelines.html">内容准则</a></nav></header><main>${content}</main><footer>静态目录 · 无账号 · 无Cookie · 无分析追踪</footer></body></html>\n`
}
const trust = (curated: boolean) => `<span class="stamp">自动校验通过</span><span class="stamp secondary">${curated ? '人工精选' : '未人工审阅'}</span>`

export async function buildStaticSite({ outputRoot }: BuildSiteOptions) {
  const index = registryIndexSchema.parse(JSON.parse(await readFile(join(outputRoot, 'registry/v1/index.json'), 'utf8')) as unknown)
  const rows = index.cases.map((item) => `<article><div><small>${escape(item.caseId)} · v${escape(item.latestVersion)}</small><h2><a href="cases/${escape(item.caseId)}/index.html">${escape(item.title)}</a></h2><p>${escape(item.summary)}</p></div><dl><div><dt>难度</dt><dd>${escape(item.difficulty)}</dd></div><div><dt>时长</dt><dd>${item.estimatedMinutes.min}—${item.estimatedMinutes.max}分钟</dd></div><div><dt>评级</dt><dd>${escape(item.contentRating)}</dd></div></dl><div>${trust(item.curated)}</div></article>`).join('\n')
  await put(join(outputRoot, 'index.html'), layout('社区案件目录', `<section class="intro"><p>LOST DESKTOP MUSEUM COMMUNITY</p><h1>档案交换站</h1><p>通过GitHub Pull Request审核和静态发布的社区案件目录。</p></section><section class="catalog" aria-label="案件目录">${rows}</section>`))
  for (const summary of index.cases) {
    const detail = registryCaseSchema.parse(JSON.parse(await readFile(join(outputRoot, summary.detailPath), 'utf8')) as unknown)
    const publisher = publisherSchema.parse(JSON.parse(await readFile(join(outputRoot, detail.publisherPath), 'utf8')) as unknown)
    const latest = detail.versions.find((item) => item.version === detail.latestVersion)!
    const screenshots = latest.screenshots.map((path) => `<img loading="lazy" src="../../${escape(path)}" alt="${escape(detail.title)}案件截图">`).join('')
    const content = `<p><a href="../../index.html">← 返回目录</a></p><section class="case-head"><div><small>${escape(detail.caseId)} · v${escape(detail.latestVersion)}</small><h1>${escape(detail.title)}</h1><p>${escape(detail.subtitle)}</p><p>${escape(detail.summary)}</p>${trust(detail.curated)}</div><dl><div><dt>发布者</dt><dd><a href="../../publishers/${escape(publisher.publisherId)}/index.html">${escape(publisher.displayName)}</a></dd></div><div><dt>引擎</dt><dd>≥ ${escape(latest.engineCompatibility.minimum)}</dd></div><div><dt>包大小</dt><dd>${latest.packageByteSize} bytes</dd></div><div><dt>许可证</dt><dd>${escape(latest.license.name)}</dd></div></dl></section><section><h2>内容提示</h2><p>${detail.contentWarnings.length ? detail.contentWarnings.map(escape).join('、') : '无特别提示'}</p><p>自动校验可以降低格式和执行风险，但不能替代对故事内容、版权和主题的人工判断。</p></section><section><h2>版本与下载</h2><p><a class="download" href="../../${escape(latest.packagePath)}" download>下载 .ldmcase</a></p><code>SHA-256 ${escape(latest.packageSha256)}</code><pre>${escape(latest.changelog)}</pre><p><a href="https://phlxxx.github.io/lost-desktop-museum/#/community/cases/${escape(detail.caseId)}" rel="noopener noreferrer">在主应用中查看详情（外部链接：phlxxx.github.io）</a></p></section><section class="screenshots">${screenshots}</section>`
    await put(join(outputRoot, `cases/${summary.caseId}/index.html`), layout(detail.title, content, 2))
    await put(join(outputRoot, `publishers/${publisher.publisherId}/index.html`), layout(publisher.displayName, `<h1>${escape(publisher.displayName)}</h1><p>${escape(publisher.description)}</p><p>GitHub资料，不代表实名认证或绝对可信。</p>`, 2))
  }
  await put(join(outputRoot, 'submission.html'), layout('投稿说明', '<h1>通过Pull Request投稿</h1><ol><li>在档案工坊导出并生成社区投稿包。</li><li>Fork社区仓库。</li><li>将文件放入建议的catalog目录。</li><li>创建Pull Request并修复自动校验结果。</li><li>等待维护者人工审核。</li></ol>'))
  await put(join(outputRoot, 'trust.html'), layout('信任模型', '<h1>信任模型</h1><h2>自动校验通过</h2><p>验证包格式、路径、资源、引用、可达性和哈希，不代表内容绝对安全。</p><h2>人工精选</h2><p>维护者检查过内容和基本体验，不代表实名认证。</p><h2>当前限制</h2><p>registry目前没有独立数字签名，客户端安装时仍会重新计算SHA-256并完整校验案件包。</p>'))
  await put(join(outputRoot, 'content-guidelines.html'), layout('内容准则', '<h1>内容准则</h1><p>禁止恶意代码、远程追踪、真实隐私、侵权素材、冒充官方、钓鱼和违法内容。虚构悬疑、恐怖、犯罪和成熟主题可以投稿，但必须准确提供内容提示。</p>'))
  await put(join(outputRoot, 'assets/community.css'), `:root{color-scheme:dark;font-family:system-ui,-apple-system,"Segoe UI",sans-serif;background:#111614;color:#e5e9e4}*{box-sizing:border-box}body{margin:0}header,footer{display:flex;justify-content:space-between;gap:24px;padding:16px 32px;border-bottom:1px solid #34403a;background:#171d1a}header a,main a{color:#b9d5c9}nav{display:flex;gap:18px}main{max-width:1120px;margin:auto;padding:36px 24px}.intro{border-bottom:1px solid #34403a;padding-bottom:28px}.catalog article,.case-head{display:grid;grid-template-columns:1fr auto auto;gap:24px;padding:24px 0;border-bottom:1px solid #34403a}.catalog h2,h1{margin:.35rem 0}.catalog dl,.case-head dl{display:grid;grid-template-columns:repeat(2,minmax(110px,1fr));gap:12px}.catalog dt,.case-head dt{font-size:12px;color:#9ca8a2}.catalog dd,.case-head dd{margin:3px 0}.stamp{display:inline-block;border:1px solid #6b9484;padding:4px 7px;margin:0 5px 5px 0;font-size:12px}.secondary{border-color:#858b76}.download{display:inline-block;border:1px solid #6b9484;padding:9px 14px}code,pre{white-space:pre-wrap;word-break:break-all;background:#0c100e;padding:12px;display:block;border:1px solid #2d3732}.screenshots{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-top:24px}.screenshots img{width:100%;height:auto;border:1px solid #34403a}footer{margin-top:40px;border-top:1px solid #34403a;border-bottom:0;color:#9ca8a2}@media(max-width:720px){header,.catalog article,.case-head{display:block}nav{margin-top:12px;flex-wrap:wrap}.screenshots{grid-template-columns:1fr}}\n`)
}
